<script setup lang="ts">
import ConceptArticle from '@/components/cascade/marketing/ConceptArticle.vue';
import MarketingLayout from '@/layouts/MarketingLayout.vue';

const blocks = [
    {
        heading: 'Атомарность каждой операции',
        body: [
            'Любое движение средств — зачисление ячейки, перелив каскада, начисление бонуса, вывод — выполняется как одна неделимая транзакция. Либо она применяется целиком, либо не применяется вовсе. Промежуточных «полусостояний» баланса не существует.',
            'Это и означает ACID: атомарность, согласованность, изолированность и устойчивость операций к сбоям. Даже если сервер прервётся посреди расчёта, очередь останется в корректном состоянии.',
        ],
    },
    {
        heading: 'Защита от состояния гонки',
        body: [
            'Счётчик ячеек блокируется на время записи через SELECT FOR UPDATE. Пока одна транзакция закрашивает ячейку, параллельные запросы ждут своей очереди.',
            'Благодаря этому одна и та же ячейка не может быть зачислена двум людям одновременно, а сумма выплат никогда не превысит сумму реальных поступлений.',
        ],
    },
    {
        heading: 'Прозрачность вместо доверия',
        body: [
            'Все операции отражаются в истории кабинета в реальном времени. Вам не нужно верить на слово: движение каждого USDT по очереди видно по шагам.',
            'Сочетание атомарных транзакций и видимой истории делает математику очереди проверяемой, а не декларативной.',
        ],
    },
];
</script>

<template>
    <MarketingLayout title="ACID-транзакции — Cascade">
        <ConceptArticle
            eyebrow="Безопасность"
            title="ACID-транзакции"
            lead="Каждая финансовая операция атомарна. Блокировка счётчика ячеек через SELECT FOR UPDATE исключает состояние гонки — одна ячейка не может достаться двум участникам сразу."
            :blocks="blocks"
        />
    </MarketingLayout>
</template>
